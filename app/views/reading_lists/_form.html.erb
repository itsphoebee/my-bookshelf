<%= form_for reading_list do |f| %>
  <%= f.label :name %>:<br>
  <%= f.text_field :name %> <br></br>

  <%= f.hidden_field :user_id, value: params[:user_id] %>

  <%= f.collection_select(:book_ids, Book.all, :id, :title,{}) %><br></br>

  <%= f.submit%>
<% end %>

<br></br>
