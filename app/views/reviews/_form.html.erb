<%= form_for ([@book, @review]) do |f| %>
  Review<br></br>
  <%= f.text_area(:content, cols: 40, rows: 10) %>

  <%= f.hidden_field :user_id, value: current_user.id%>
  <%= f.hidden_field :book_id, value: params[:book_id]%><br>

  <%= f.submit %>

<% end %>

<br></br>
