<h1>All Books</h1>
<%= link_to 'New Book', new_book_path(@book), class: "btn btn-primary"%>

<ul>
  <% @books.each do |b| %>
    <li>
      <div id ="booktitle">
      <%= link_to b.title, book_path(b) %>
    </div>
    by <%= b.author.name %></li>
  <% end %>
</ul>
