<h1>Most Popular Books</h1>
<div id ="trending">
  <ol>
    <% @books.each do |b|%>
      <li><%= link_to b.title, book_path(b) %> by <%= link_to b.author.name, author_path(b.author) %></li>
    <% end %>
  </ol>
</div>
