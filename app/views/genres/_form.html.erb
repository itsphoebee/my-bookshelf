<%= form_for genre do |f| %>
  <%= f.label :name %>:<br>
  <%= f.text_field :name %> <br></br>

  <%= f.label :description %>:<br>
  <%= f.text_area(:description, cols: 40, rows: 10)%><br></br>

  <%= f.submit%>
<% end %>
