<% if @genre.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@genre.errors.count, "error") %> prohibited this item from being saved:</h2>

    <ol>
      <% @genre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ol>
  </div>
<% end %>

<%= form_for @genre do |f| %>
  <%= f.label :name %>:<br>
  <%= f.text_field :name %> <br></br>

  <%= f.label :description %>:<br>
  <%= f.text_area(:description, cols: 40, rows: 10)%><br></br>

  <%= f.submit%>
<% end %>

<br></br>
