<h1>All Genres</h1>

<ul>
  <% @genres.uniq.each do |g| %>
    <li>
      <div id ="genrename">
        <%= g.name %>
      </div>
    Total Number of Books in Genre: <%= g.books.count %>
    </li>
  <% end %>
</ul>
